{
  "name": "exokit-site",
  "version": "0.0.1",
  "private": true,
  "homepage": "https://new.webmr.io",
  "dependencies": {
    "bootstrap": "^4.1.1",
    "gh-pages": "^1.1.0",
    "react": "^16.3.2",
    "react-bootstrap": "^0.32.1",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build && cp CNAME build/ && touch build/.nojekyll",
    "deploy": "gh-pages -d build"
  }
}
